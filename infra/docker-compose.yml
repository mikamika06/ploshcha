# Dev-оркестрація. Наповнюється, коли зʼявиться services/sim (Фаза 4).
# НІКОЛИ не вкладати сюди ваги моделей чи бази — тільки сервіси застосунку.
services: {}
#  web:            # apps/web (Vite dev) — Фаза 2c
#  sim:            # services/sim (FastAPI SSE) — Фаза 4
